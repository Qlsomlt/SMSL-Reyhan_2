Grafana configuration placeholder.

After starting the stack, open http://localhost:3000 and log in with:
- username: admin
- password: admin

You can add a Prometheus data source with URL http://prometheus:9090 and create a dashboard for the metric prediction_total.
